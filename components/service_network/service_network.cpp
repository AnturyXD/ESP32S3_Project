#include "service_network.h"

#include "esp_log.h"

static const char *TAG = "WIFI";

esp_err_t service_network_init(void)
{
    ESP_LOGI(TAG, "service_network initialized (skeleton)");
    return ESP_OK;
}
