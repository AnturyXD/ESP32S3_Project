#include "service_audio.h"

#include "esp_log.h"

static const char *TAG = "AUDIO";

esp_err_t service_audio_init(void)
{
    ESP_LOGI(TAG, "service_audio initialized (skeleton)");
    return ESP_OK;
}
